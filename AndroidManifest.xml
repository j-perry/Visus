<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.visus"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        >
        
        <activity
            android:name="com.visus.main.MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation"
            android:screenOrientation="portrait" 
            >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- new user -->
        <activity 
            android:name="com.visus.main.SignUp"
            android:label="@string/title_activity_new_user" 
            />
                
        <!-- new session -->
        <activity
            android:name="com.visus.main.NewSession"
            android:label="@string/activity_new_session" 
            />
                
        <!-- previous sessions -->
        <activity
            android:name="com.visus.main.Sessions"
            android:label="@string/title_activity_prev_sessions" 
            />
        
        <!-- latest activity -->
        <activity
            android:name="com.visus.main.LatestActivity"
            android:label="@string/title_latest_activity" 
            />
        
        <!-- activities -->
        <activity
            android:name="com.visus.main.Activities"
            android:label="@string/title_activities" 
            />
        
        <!--  -->
        <activity
            android:name="com.visus.main.ActivityCategory"
            android:label="@string/title_activity_category" 
            />
        
        <!-- activity category -->
        <activity
            android:name="com.visus.main.UserActivity"
            android:label="@string/title_activity" 
            />
        
        <!-- view settings -->
        <activity
            android:name="com.visus.main.Settings"
            android:label="@string/title_activity_view_settings"
            />
        
    </application>

</manifest>