<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androiddemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" 
        >
        
        <activity
            android:name="com.example.androiddemo.MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation"
            android:screenOrientation="portrait" 
            >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- new session -->
        <activity
            android:name="com.example.androiddemo.NewSession"
            android:label="@string/title_activity_new_session" >
        </activity>
        
        <!-- previous sessions -->
        <activity
            android:name="com.example.androiddemo.PrevSessions"
            android:label="@string/title_activity_prev_sessions" >
        </activity>
        
        <!-- view settings -->
        <activity
            android:name="com.example.androiddemo.ViewSettings"
            android:label="@string/title_activity_view_settings" >
        </activity>
        
    </application>

</manifest>